import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
  <div className={"md:dark:hover:text-amber-800 bg-[url('/vercel.svg')]"}>
    <span className={"text-[234px] text-[#eeffaa]"}>check the functions of JIT compiler</span>
  </div>
  );
};

export default Home;
